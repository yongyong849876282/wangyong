server:
  port: 6661

spring:
  activemq:
    broker-url: tcp://129.28.167.240:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false # (默认)false = Queue  true = Topic

#自己定义队列名称
myqueue: boot-activemq-queue